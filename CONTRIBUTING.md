# Contributing

Use Node.js 20 or newer. Keep changes issue-sized and golden-path focused.

Before committing, run:

```bash
npm run check
```

Every change should state its acceptance criteria, tests run, known limitations, and whether it expands scope. Scope expansions must be recorded in `docs/build-journal.md` before implementation.

