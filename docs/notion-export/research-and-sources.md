# Research and sources

Selected `json5@1.0.1` and OSV/GitHub advisory `GHSA-9c47-m6qq-7p4h` after a live OSV API comparison on 2026-07-18. It produces one finding, has fixed 1.x/2.x releases, and maps to the fixture's direct `JSON5.parse` call. See ADR 0001. The official OSV-Scanner v2.3.8 release provides prebuilt macOS and Linux binaries; the exact setup is finalized in Issue 3.
